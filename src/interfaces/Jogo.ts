export interface IJogo {
    id: number,
    titulo: string;
    desenvolvedora: string;
    plataforma: string;
    data_lancamento: string;
    preco: number;
    descricao: string;
    quantidade: number;
}